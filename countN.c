#include <stdio.h>
#include <string.h>
#include <math.h>

int main(){
    // char name[35];
    int N;

    printf("Enter the value of N: ");  
    scanf("%d", &N);
    getchar();
  
    for(int i = 1; i <= N; i++){
        printf("%d \n", i);
        
        
    }
    return 0;
}
